@model ecoles_informatiques.Models.ViewModels.FormationViewModel

@{
    ViewData["Title"] = "Détails de la formation";
}

<div class="container mt-5">
    <div class="text-center">
        <h1 class="mb-4">@Model.Formation.Name <h1>
    </div>

                </li>
                <li class="list-group-item">
                    <strong>Description :</strong> @Model.Formation.Description
                </li>
                @if (Model.Formation.ApprenticeshipStatus)
                {
                    <li class="list-group-item">
                        <strong>Alternance :</strong> Cette formation est en alternance
                    </li>
                }
                else
                {
                    <li class="list-group-item">
                        <strong>Alternance :</strong> L'alternance n'est pas possible
                    </li>
                }
                @if (Model.Formation.StudentStatus)
                {
                    <li class="list-group-item">
                        <strong>Statut étudiant :</strong> La formation permet le statut étudiant
                    </li>
                }
                <li class="list-group-item">
                    <strong>Établissement :</strong>
                    <a href="/ecoles">@Model.School.Name</a>
                </li>
            </ul>
            <div class="mt-4 text-center">
                <a href="/ecoles" class="btn btn-primary">Retour à la liste des écoles</a>
            </div>
        </div>
    </div>
</div>
